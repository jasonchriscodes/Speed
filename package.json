{
  "name": "speed",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client:setup": "cd client && npm install",
    "client:build": "cd client && npm run build",
    "server:setup": "npm install",
    "heroku-postbuild": "npm run server:setup && npm run client:setup && npm run client:build",
    "start": "concurrently --kill-others-on-fail \"npm run start --prefix backend\" \"npm run start --prefix client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
