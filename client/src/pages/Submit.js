import React from 'react';

const Submit = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Submit</h1>
    </div>
  );
};

export default Submit;
